[build]
dockerfilePath = "docker/chatwoot/Dockerfile"

[deploy]
startCommand = "sh -c 'bundle exec sidekiq & bundle exec rails s -p ${PORT:-3000} -b 0.0.0.0'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3