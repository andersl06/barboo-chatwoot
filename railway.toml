[build]
dockerfilePath = "docker/chatwoot/Dockerfile"

[deploy]
startCommand = "bundle exec rails s -p $PORT -b 0.0.0.0"
healthcheckPath = "/auth/sign_in"
healthcheckTimeout = 600
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3